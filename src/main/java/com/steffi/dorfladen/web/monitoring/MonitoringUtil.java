package com.steffi.dorfladen.web.monitoring;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class MonitoringUtil {

    // @Bean
    // DBMonitoringFrame dbMonitoringFrame(DBMonitoringController controller) {
    //     return new DBMonitoringFrame(controller);
    // }

    // @Bean
    // MonitoringFrame monitoringFrame(DBMonitoringFrame dbMonitoringFrame) {
    //     return new MonitoringFrame(dbMonitoringFrame);
    // }
}
